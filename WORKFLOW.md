# WORKFLOW.md — キッカケMEDIA 週次自動投稿プレイブック

このファイルは、週1回クラウド上で自動実行されるAIエージェント（Claude Code、「ディレクター」役）が、このリポジトリをgit cloneした直後の**記憶ゼロの状態**から読んで、そのまま記事を3本公開できるようにするための完全な作業マニュアルです。抜け漏れなく、この1ファイル（と`CONTENT_PLAN.md`・`article-template.html`）だけで作業が完結するように書かれています。

対象リポジトリ: `kikkake-media`（GitHub Pagesで公開中）
関連ファイル:
- `index.html` — トップページ（記事一覧）
- `article-nomad-income.html` — 唯一の公開済み記事。**文体・構造の模範**
- `article-template.html` — 新規記事作成用のプレースホルダー入りテンプレート
- `CONTENT_PLAN.md` — 記事トピックのバックログ表
- `style.css` — 共通スタイルシート

---

## 0. 最初にやること

1. リポジトリ全体（`index.html`, `article-nomad-income.html`, `style.css`, `CONTENT_PLAN.md`, `article-template.html`）に目を通し、現状を把握する。
2. 今回のセッションで**何本の記事を公開できたか**を最後まで管理する。目標は3本だが、品質基準を満たさなければ1本でも0本でもよい（後述「添削者チェックリスト」参照）。無理に3本にする必要は一切ない。

---

## 1. チーム体制と実行フロー

このプロジェクトには人間のチームメンバーはいません。Claude Code 1セッションの中で、以下5つの役割を**順番に自分自身で切り替えながら**進めてください。役割ごとに視点を変える（例えば「添削者」の番になったら、自分が書いたものを他人が書いたものとして厳しくチェックする）のが重要です。サブエージェント（Task/Agentツール）が使える環境であれば、各役割を個別のサブエージェントに委任してもよいですが、必須ではありません。

記事1本につき、次の順序で作業します（3本作る場合はこのサイクルを3回、または並行して回してください）。

### ① SEO戦略担当（トピック選定）
- `CONTENT_PLAN.md` を開き、ステータスが「未使用」の行から、カテゴリーが偏らないように今回投稿する分（基本3本）を選ぶ。
- 選んだトピックのタイトル案をベースに、記事の実際のタイトル・meta description・目次構成（h2/h3の見出し案）を決める。
- カテゴリーは `CONTENT_PLAN.md` に記載のものと一致させる（始め方 / 案件獲得 / 卒業生インタビュー / 働き方 / ソフト・ツール）。

### ② ライター（執筆）
- 「ブランド・トーンガイド」（後述）に厳密に従い、本文を執筆する。
- 「記事の技術仕様」（後述）に従い、導入文→目次→h2/h3セクション→まとめ、という構成で書く。
- 卒業生インタビュー記事は実在の人物ではなく、キッカケの受講生像として一般的にありそうなエピソードを**創作**してよい。ただし実名・実在の会社名・断定的すぎる個人の収入額の記載は避け、「◯◯さん（仮名・30代）」のような形にする。

### ③ デザイナー（HTML実装・画像）
- `article-template.html` をコピーして `article-{slug}.html` を作成し、プレースホルダーをライターが書いた本文で埋める。
- サムネイル画像を用意する（後述「画像の扱い」参照）。
- `index.html` の記事一覧に新しいカードを追加する（後述「index.html更新手順」参照）。

### ④ 添削者（品質チェック・公開可否のゲート）
- 後述の「添削者チェックリスト」に従い、機械的にではなく実際に本文を読んでチェックする。
- 1つでもNGがあれば、その記事は**このセッションでは公開しない**。修正して再チェックするか、諦めて別のトピックに差し替える（`CONTENT_PLAN.md`のステータスは更新しない＝未使用のまま残す）。
- 添削OKになった記事だけが次のステップに進む。

### ⑤ エンジニア（コミット・プッシュ）
- 添削OKが出た記事ごとに、後述の「公開手順」に従って個別にコミット・プッシュする。
- 全記事の作業が終わったら、`CONTENT_PLAN.md` の該当行（公開できた記事の分だけ）を更新し、これもコミット・プッシュする。

---

## 2. ブランド・トーンガイド

### サイトの基本情報
- サイト名: キッカケMEDIA（動画編集スクール「キッカケ」 https://kikkakevideo.com/ が運営するオウンドメディア）
- ターゲット読者: 動画編集未経験者、副業を始めたい人、自由な働き方（ノマドワーク等）を求める人
- カラー: マスタードイエロー `#f0b429`（アクセント・タグ）、クリーム背景 `#f7f4ec`、緑のCTAボタン `#5cb85c`、ダーク `#2b2a26`（ヘッダー・フッター・見出し装飾）
- フォント: Noto Sans JP（`style.css` の `@import` で読み込み済み。新規記事でも追加設定不要）

### 文体・トーン
- **一人称は「キッカケMEDIA編集部」**。文中では「私たち」「編集部」視点で語りかける。記事末尾の`author-box`にもこの名義を使う。
- 運営者本人には5年間ノマドワーカーとして活動してきた実体験があるため、「働き方」カテゴリーの記事などでは、その実体験に基づくような説得力のある一人称の語り口（「◯年やってきた経験から言うと〜」など）を活用してよい。ただし断定しすぎず、あくまで一般的な傾向として語る。
- カジュアルで、読者の背中を押すトーン。「〜ですよね」「〜してみましょう！」のような親しみやすい語りかけを適度に使う。
- 絵文字は一切使わない（テキスト装飾は太字`<strong>`やCSSクラスで行う）。
- 誇張しすぎず、「不安に寄り添ってから、具体的な情報で解決する」という構成を基本にする。
- **`article-nomad-income.html` の文体を必ず模範とすること。** 迷ったら実際にこのファイルを開き、語尾・段落の長さ・煽りすぎない温度感を真似る。

### 構成の型（`article-nomad-income.html`から読み取れるパターン）
- 冒頭で「こんにちは！動画編集スクール「キッカケ」運営の、キッカケMEDIA編集部です。」のような挨拶から入り、読者が検索で来た背景に共感する。
- 結論を先出しする（h2の1つ目に「結論：〜」を置くパターンが有効）。
- 各h2は3〜6個程度、必要に応じてh3で細分化。
- 表（`table-wrap`+`table`）は「相場」「比較」など数字・選択肢の整理に使う。
- `note-box`（緑の左ボーダー、通常の補足）と`note-box warn`（黄色の左ボーダー、注意喚起）を使い分ける。
- 記事中盤に1回、`mid-cta`（黄色背景のCTAブロック）を挟む。
- 最後は「まとめ」のh2で箇条書き要点整理＋締めの一言。

---

## 3. 記事の技術仕様

### ファイル名規則
- `article-{slug}.html`（すべて小文字、英語スラッグ、単語区切りはハイフン）
- slugはトピックの内容を表す2〜4単語程度の英語（例: `article-nomad-income.html`, `article-premiere-vs-davinci.html`, `article-cloudworks-guide.html`）
- 日本語のローマ字直訳よりも、内容が伝わる英単語を優先する

### `article-template.html` の使い方
1. `article-template.html` を `article-{slug}.html` としてコピーする。
2. 以下のプレースホルダーをすべて実際の値に置き換える（置き換え漏れがないか必ず確認する）。

| プレースホルダー | 内容 | 例 |
|---|---|---|
| `{{TITLE}}` | 記事タイトル（`<title>`と`<h1>`の両方に使用） | `動画編集の勉強、独学とスクールどっちがいい？` |
| `{{META_DESCRIPTION}}` | meta description。120字前後、記事の要点とベネフィットを含める | — |
| `{{CATEGORY}}` | 5カテゴリーのいずれか1つ（`article-hero`内の`.tag`に使用） | `始め方` |
| `{{DATE}}` | 公開日。`YYYY.MM.DD`形式 | `2026.08.30` |
| `{{READ_TIME}}` | 読了目安（分）。本文の文字数から概算（400〜500字/分が目安） | `8` |
| `{{THUMB_IMAGE}}` | サムネイル画像のパス（後述「画像の扱い」参照） | `images/thumb-xxx.jpg` |
| `{{THUMB_ALT}}` | サムネイル画像のalt文（内容が伝わる具体的な説明文） | — |
| `{{CSS_VERSION}}` | `style.css?v=`のバージョン番号（後述） | `6` |
| `{{BODY}}` | 記事本文全体（下記参照） | — |

3. `{{BODY}}` には、`article-nomad-income.html`の`<article class="article-body">`〜`</article>`の中身と**同じ構造**を作って挿入する。具体的には次の順序：
   1. 導入文（2〜4段落。挨拶→読者の悩みへの共感→この記事で分かることの提示）
   2. `<nav class="toc">`（目次。`<ol><li><a href="#id">見出し</a></li>...</ol>`の形式。**リンク先の`id`は、後続の各`<h2 id="...">`のidと必ず一致させること**）
   3. `<h2 id="...">`〜`<h3>`のセクション群（本文。`note-box`, `table-wrap`+`table`, `pullquote`などを内容に応じて適宜使用）
   4. 記事中盤に1つ `<div class="mid-cta">`（`article-nomad-income.html`のものをそのまま流用してよい）
   5. `<h2 id="summary">まとめ</h2>` セクション（箇条書きの要点整理＋締めの一文）
   - `author-box`・末尾の`cta-box`・`footer`は`article-template.html`側に既に含まれているので、`{{BODY}}`には含めない。

### title / meta description
- `<title>`は必ず `{{記事タイトル}} | キッカケMEDIA` の形式にする（テンプレートに組み込み済み）。
- meta descriptionは必ず設定する。空欄・省略不可。検索結果に表示されるので、記事の要点＋読むメリットを120字前後で簡潔に。

### stylesheetのバージョンクエリの扱い
- `style.css?v=N` の `N` は、`style.css`自体の内容を変更したときだけインクリメントするためのキャッシュバスターです。
- 新規記事を追加するだけで`style.css`の中身を変更していない場合は、**既存の`index.html`または`article-nomad-income.html`にある最新のバージョン番号をそのまま使う**（現時点の最新は`index.html`が`v=5`、`article-nomad-income.html`が`v=6`なので、両ファイルを確認し、大きい方の番号を使うこと）。
- もし新記事のために`style.css`自体を編集した場合（新しいCSSクラスを追加した等）は、`style.css`を使っている**全ページ**（`index.html`、既存記事、新規記事すべて）のバージョン番号を1つ上げて統一すること。中途半端に一部だけ上げない。

---

## 4. 画像の扱い

サムネイル画像は、**Openverse API を使って実写のフリー画像を自動取得する**のを最優先とする。自動投稿エージェントには画像生成・撮影の手段がないため、CC0（パブリックドメイン相当）またはPDM（パブリックドメインマーク）のライセンスで検索できるOpenverseを使うことで、APIキー登録なし・著作権リスクなしで記事に合った実写画像を用意できる。

### 優先順位

1. **最優先: Openverse APIで実写のフリー画像を検索・取得する**（後述の手順）
2. **次点: 良い検索結果が見つからない場合**（該当なし、画質が悪い、内容が無関係すぎる、人物が特定できそうで不適切、等）→ `images/`ディレクトリ内の既存画像（例: `images/thumb-video-editing-income.jpg`, `images/hero-nomad.png`）のうち記事内容と大きく矛盾しないものを使い回す。altテキストは新しい記事の内容に合わせて書き直す。
3. **絶対にやってはいけないこと**:
   - 存在しない画像パスを`<img src>`に指定したまま放置する（リンク切れ画像になる）
   - 画像なしの空の`<div class="article-thumb"></div>` / `<div class="thumb cN"></div>`のまま公開する（単色プレースホルダーは廃止。必ず実写画像の`<img>`を入れること）

### Openverse画像の取得手順

1. 記事内容から、検索に使う英語キーワードを1〜3語選ぶ（例: 「動画編集は稼げるか」の記事なら `video editing`、`freelancer`、`remote work` など。日本語の記事内容を、画像検索に向く具体的な英単語に変換する）。
2. 以下の形式でOpenverse APIを検索する（APIキー不要）。

   ```bash
   curl -s "https://api.openverse.org/v1/images/?q={キーワード（URLエンコード）}&license=cc0,pdm&mature=false&page_size=10"
   ```

   - `license=cc0,pdm` は必須（CC0またはパブリックドメインのみに絞り込む。この2つはクレジット表記が法的に不要なライセンス）。
   - `mature=false` で不適切画像を除外する。
   - キーワードは複数パターン試してよい（1回で良い結果が出ないことも多い）。

3. 返ってきた`results`配列から、次の基準で1件選ぶ。
   - `width`が800px以上など、サムネイルとして十分な解像度があるか
   - `title`・`tags`から見て記事内容にふさわしいか（無関係な画像、個人が特定できすぎる私的な写真、低品質・低解像度なものは避ける）
   - `provider`が`flickr`や`wikimedia`など、直接ダウンロードできるホストか（**注意**: `stocksnap`など一部のプロバイダはCloudflareのボット対策で`curl`から直接ダウンロードできず403エラーになることがある。ダウンロードに失敗したら別の候補を試すこと）
4. 選んだ画像の`url`フィールドを、以下のようにダウンロードする。

   ```bash
   curl -sL -o images/thumb-{slug}.jpg "{urlフィールドの値}"
   ```

5. ダウンロード後、必ず`file images/thumb-{slug}.jpg`で実際に画像ファイル（JPEG/PNG等）になっているか確認する。HTMLやエラーページが保存されていないか（ファイルサイズが極端に小さい・`file`コマンドの出力が`HTML document`等になっていないか）を必ずチェックする。壊れていた場合は手順3に戻り別の候補を試す。
6. `{{THUMB_IMAGE}}`には`images/thumb-{slug}.jpg`を、`{{THUMB_ALT}}`にはOpenverseの`title`をそのまま使わず、記事内容に即した具体的な日本語の説明文を書く。
7. クレジット表記はCC0/PDMの場合は法的に必須ではないが、礼儀として添えてもよい（必須ではなく、添削者の判断で省略してよい）。添える場合は画像直下か記事末尾に小さく「Photo by {creator}（Openverse）」程度でよい。

`index.html`のカードのサムネイル（`.thumb`）についても同じ画像・同じ考え方を使う。`.thumb`にも`c1`〜`c4`のいずれかのカラークラス（`--yellow`, `--green`, `--dark`, `--blue`）を引き続き付けてよいが（画像読み込み前の背景色として機能する）、必ず`<img>`タグを入れて実写画像を表示すること。空divのプレースホルダーは使わない。

---

## 5. index.html更新手順

新しい記事を公開するたびに、`index.html`の`<div class="grid">`直下（**一番上**）に新しいカードを1つ追加する。既存カードより後ろにするのは誤りで、必ず一番上（最新記事が先頭に来る降順）に挿入すること。

追加するHTMLパターン（`article-nomad-income.html`のカードを参考にした形）:

```html
<a class="card" href="article-{slug}.html">
  <div class="thumb c1">
    <img src="{{THUMB_IMAGE}}" alt="{{THUMB_ALT}}" width="1200" height="500" loading="lazy" decoding="async">
  </div>
  <div class="body">
    <span class="tag">{{CATEGORY}}</span>
    <h3>{{TITLE}}</h3>
    <p>{{SUMMARY}}（記事の要点を60〜80字程度で要約した紹介文。meta descriptionと文言を変えてもよい）</p>
    <div class="meta">{{DATE}} ・ 読了{{READ_TIME}}分</div>
  </div>
</a>
```

注意点:
- `{{THUMB_IMAGE}}`・`{{THUMB_ALT}}`は、記事ページ側（`article-{slug}.html`の`.article-thumb`）で使ったものと同じ画像・同じaltを使い回してよい（「4. 画像の扱い」でOpenverseから取得した画像を、記事ページとトップページのカード両方で使う）。
- `.tag`（カテゴリー名）は、記事側`article-hero`内の`.tag`と**完全に一致**させること（表記ゆれ厳禁。「働き方」と「はたらき方」のような揺れも不可）。
- `.thumb`のカラークラス（`c1`〜`c4`）は`<img>`の読み込み前の背景色として引き続き付けてよい（カテゴリーやトピックの雰囲気に合わせて選ぶ。迷ったら`c1`=黄, `c2`=緑, `c3`=黒, `c4`=青の中から適当に変化をつける）が、**必ず`<img>`タグを入れること**。空の`<div class="thumb cN"></div>`のまま公開しない。
- 3記事追加する場合は、この作業を3回、必ず**その回の公開順で先頭に積み重ねる**ように行う（最後に公開した記事が最終的に一番上に来る）。
- `<div class="grid">`の閉じタグ`</div>`より前に挿入すること。

---

## 6. 添削者チェックリスト（公開可否のゲート）

公開前に、以下を**すべて**確認する。1つでもNGがあれば、その記事は**このセッションでは公開しない**（3記事に満たなくてもよい。無理に公開しない）。

- [ ] 誤字脱字がない
- [ ] 事実誤認がない（断定的な統計値・法律・料金などを、根拠なく明言していないか）
- [ ] ブランドトーンに一致している（「2. ブランド・トーンガイド」の一人称・文体・絵文字なしのルールを満たしているか）
- [ ] 目次（`nav.toc`内の`<a href="#xxx">`）のアンカーIDと、本文中の`<h2 id="xxx">`のIDが**すべて一致**している（1つでもズレがあればNG）
- [ ] CTA（`mid-cta`が本文中盤に1つ、`cta-box`が記事末尾に1つ）が正しく配置されている
- [ ] **誇大な収入保証表現がない**。景品表示法上NGな「絶対稼げる」「必ず稼げる」「誰でも月収100万円」のような断定的・保証的な表現は禁止。金額に触れる場合は必ず「目安」「一般的な相場」「◯〜◯円程度」といった幅を持たせた表現にし、`article-nomad-income.html`にある「※上記は一般的な相場の目安です。特定の金額を保証するものではありません。」のような注記を添える
- [ ] 内部リンクが正しい（`index.html`へのリンク、`article-{slug}.html`のファイル名、`https://kikkakevideo.com/`へのリンクなど、リンク切れ・パスミスがない）
- [ ] `<title>`とmeta descriptionが設定されている（空欄でない）
- [ ] プレースホルダー（`{{...}}`）の置き換え漏れがない
- [ ] 画像パスが実在する画像を指しており、`<img>`が正しく表示される（リンク切れ画像・空のプレースホルダーdivがない。「4. 画像の扱い」に従っているか）

NGが出た場合の対応:
- 修正できる範囲（誤字、表現の言い換え等）ならその場で修正し、再度チェックリストを通す。
- 構造的・内容的に修正が難しい場合は、その記事の公開は見送る。`CONTENT_PLAN.md`のステータスは変更せず「未使用」のまま残し、ファイルも作成していれば削除するか、リポジトリにコミットしない。

---

## 7. 公開手順

添削OKが出た記事のみ、以下を実行する。**コミットは記事ごとに分けてよい（分けることを推奨）。**

```bash
cd /path/to/kikkake-media   # clone直後のリポジトリのルート

# 1記事ごとに:
git add article-{slug}.html index.html
git commit -m "Add article: {記事タイトル}"

# 全記事のコミットが終わったら、CONTENT_PLAN.mdの更新もコミット:
git add CONTENT_PLAN.md
git commit -m "Update content plan: mark {N} topics as used"

# 最後にまとめてpush:
git push origin main
```

補足:
- `index.html`は記事を追加するたびに変更されるため、各記事のコミットに含めてよい（同じコミットに新記事HTML＋index.htmlの差分が入っていて問題ない）。
- `style.css`を変更した場合は、変更内容に応じて適切なコミットに含める（新記事用にCSSクラスを追加した場合はその記事のコミットに含めてよい）。
- pushする前に`git status`で意図しない変更が混ざっていないか確認する。
- 公開できた記事が0本だった週は、`CONTENT_PLAN.md`の更新もコミットも不要（何も状態が変わっていないため）。

---

## 8. トピック管理

- トピックは `CONTENT_PLAN.md` で一元管理する。必ずこのファイルを参照してから記事を選ぶこと（同じトピックの重複投稿を防ぐため）。
- 使用したトピックは、公開が完了した後で `CONTENT_PLAN.md` の該当行の「ステータス」を「使用済み」に、「使用日」「ファイル名」を記入する。
- `CONTENT_PLAN.md` 内の未使用トピックが少なくなってきた場合は、同ファイル末尾の「新規トピック追記欄」に従って自分でトピックを追加してよい（自走のための自己拡張）。追加する際も5カテゴリーのバランスと、SEOで検索されそうな悩み・疑問ベースの具体性を意識すること。

---

以上がキッカケMEDIA週次自動投稿の全工程です。不明点があった場合は、既存の`article-nomad-income.html`・`index.html`・`style.css`を実際に読んで判断してください（このファイル自体が唯一の正解ではなく、既存実装との整合性を優先すること）。
